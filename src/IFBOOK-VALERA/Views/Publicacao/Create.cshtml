@model IFBOOK_VALERA.Models.Publicacao

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>
<meta charset="utf-8" />
<form asp-action="Create">
    <div class="form-horizontal">
        <h4>Publica��o</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="DataPublicacao" value="@DateTime.Now" class="form-control" />
        <div class="form-group">
            <label asp-for="Descricao" class="col-md-2 control-label"></label><br />
            <div class="col-md-10">
                <textarea asp-for="Descricao" class="form-control"></textarea>
                <span asp-validation-for="Descricao" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
